import React, { FC } from 'react';
import { Col, Form, Input, Row } from 'antd';
import { uuid } from '../../utils/uuid';
import ActionTaskCollapse from './ActionTaskCollapse';

interface Props {
    scriptParams: any;
}
const ActionParams: FC = props => {
    return <Row />;
};
export default ActionParams;
